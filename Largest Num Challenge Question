## Write a function largestNum that finds the largest of three numbers.

def largestNum(num1, num2, num3):
  # Check is num1 is the largest
  if (num1 >= num2) and (num1 >= num3):
    largest = num1
  # Check is num2 is the largest
  elif (num2 >= num1) and (num2 >= num3):
    largest = num2
  else:
    largest = num3

  return largest

